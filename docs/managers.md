# Менеджеры

* [Справочник типов и прав менеджера](#dict)
* [Добавление менеджера](#add)
* [Редактирование менеджера](#edit)
* [Удаление менеджера](#delete)

Для получения информации необходимо авторизоваться под работодателем. 
Без авторизации или с неправильной авторизацией вернёт `403 Forbidden`.

<a name="dict"></a>
## Справочник типов и прав менеджера

### Запрос
`GET /employers/{employer_id}/manager_types`, где employer_id - идентификатор менеджера
 
### Ответ

Пример ответа:
```json
{
    "managers": [
        {
            'id': 'manager',
            'name': 'Менеджер',
            'allowed_permissions:': [
                {
                    'id': 1,
                    'name': 'can_create_vacancy'
                },
                {
                    'id': 2,
                    'name': 'can_view_resume_contacts'
                }
            ]
        }
    ]
}
```json

 Имя | Тип | Описание
 --- | --- | ---
 id | строка | идентификатор типа менеджера
 name | строка | название типа менеджера
 allowed_permissions | список | список доступных прав
 
В элементе allowed_permissions содержится список доступных прав:

 Имя | Тип | Описание
 --- | --- | ---
 id | строка | идентификатор права
 name | строка | название права
 
### Ошибки
* `404 Not found` - Работодатель не найден или у пользователя нет прав

<a name="add"></a>
## Добавление менеджера

### Запрос
`POST /employers/{employer_id}/managers`, где employer_id - идентификатор менеджера

На вход подается json. Пример:

```json
{
    'last_name': '',
    'first_name': '',
    'middle_name': '',
    'manager_type': '',
}
```json

### Ответ

### Ошибки

<a name="edit"></a>
## Редактирование менеджера

### Запрос

### Ответ

### Ошибки

<a name="delete"></a>
## Удаление менеджера

### Запрос

### Ответ

### Ошибки